# NLP Real Reference Project

This project evaluates transparent sentiment classifiers on the UCI Sentiment
Labelled Sentences corpus. The three public review sources are fixed evaluation
units; content hashes define the immutable split, and each trial preserves
hashed per-example predictions. This is real integration evidence for the
pipeline, not a claim that the small benchmark is itself an ACL paper.
