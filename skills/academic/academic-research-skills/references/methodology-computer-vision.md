# Computer Vision Methodology

Record dataset versions and checksums, split provenance, preprocessing,
augmentation, resolution, labels, pretrained assets, training/tuning budgets,
seeds, and evaluator identity. Check duplicates and pretraining overlap. Report
task metrics with uncertainty plus calibration, robustness, subgroups, latency,
throughput, memory, and compute when relevant. Select qualitative successes and
failures by a declared reproducible policy.
