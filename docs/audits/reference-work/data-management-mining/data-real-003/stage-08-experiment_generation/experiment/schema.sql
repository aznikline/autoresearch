CREATE TABLE rides (
  instant INTEGER PRIMARY KEY, dteday TEXT NOT NULL, season INTEGER NOT NULL,
  yr INTEGER NOT NULL, mnth INTEGER NOT NULL, hr INTEGER NOT NULL,
  holiday INTEGER NOT NULL, weekday INTEGER NOT NULL, workingday INTEGER NOT NULL,
  weathersit INTEGER NOT NULL, temp REAL NOT NULL, atemp REAL NOT NULL,
  hum REAL NOT NULL, windspeed REAL NOT NULL, casual INTEGER NOT NULL,
  registered INTEGER NOT NULL, cnt INTEGER NOT NULL
);
